---
services:
 node:
  image: node:24
  stdin_open: true  # docker run -i
  tty: true         # docker run -t
  hostname: node
networks:
 default:
  enable_ipv6: true
  ipam:
   config:
    - subnet: 10.255.82.96/29
