---
services:
 fedora:
  image: quay.io/fedora/fedora:44
  stdin_open: true  # docker run -i
  tty: true         # docker run -t
networks:
 default:
  enable_ipv6: true
  ipam:
   config:
    - subnet: 10.255.209.208/29
