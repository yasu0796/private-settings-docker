---
services:
 ol9:
  image: oraclelinux:9
  stdin_open: true  # docker run -i
  tty: true         # docker run -t
networks:
 default:
  enable_ipv6: true
  ipam:
   config:
    - subnet: 10.255.136.80/29
