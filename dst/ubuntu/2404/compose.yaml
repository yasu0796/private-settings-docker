---
services:
 ubuntu2404:
  image: ubuntu:24.04
  stdin_open: true  # docker run -i
  tty: true         # docker run -t
networks:
 default:
  enable_ipv6: true
  ipam:
   config:
    - subnet: 10.255.126.208/29
